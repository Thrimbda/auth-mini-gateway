{
  "name": "auth-mini-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run tests",
    "start": "node dist/src/server.js",
    "upstream": "node dist/examples/upstream.js",
    "mock:auth-mini": "node dist/examples/mock-auth-mini.js",
    "smoke:nginx": "node scripts/smoke-nginx.mjs"
  },
  "dependencies": {
    "jose": "^6.1.3",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  }
}
